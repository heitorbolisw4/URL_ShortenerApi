services:
  api:
    build:
      context: .
      dockerfile: Dockerfile
    image: url-shortnet-api:dev
    ports:
      - "5198:8080"
    environment:
      ASPNETCORE_ENVIRONMENT: Development
      ASPNETCORE_URLS: http://+:8080
